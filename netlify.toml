[build]
  publish = "."
  command = "npm install"

[build.environment]
  NODE_VERSION = "18"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Note: Scheduled reminder sending can be re-enabled by adding:
# [functions."send-checkin-reminders"]
#   schedule = "@hourly"

