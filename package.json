{
  "name": "zique-fitness-nutrition",
  "version": "1.0.0",
  "description": "Zique Fitness Nutrition - AI-powered meal planning platform",
  "scripts": {
    "dev": "netlify dev",
    "dev:spa": "vite",
    "build": "vite build",
    "build:mobile": "rm -rf www && mkdir -p www/css www/icons www/js && cp *.html www/ && cp manifest.json www/ && cp sw.js www/ && cp -r css/* www/css/ && cp -r icons/* www/icons/ && cp -r js/* www/js/",
    "cap:sync": "npm run build:mobile && npx cap sync",
    "cap:ios": "npm run build:mobile && npx cap sync ios && npx cap open ios",
    "cap:android": "npm run build:mobile && npx cap sync android && npx cap open android",
    "android:build-debug": "npm run cap:sync && cd android && ./gradlew assembleDebug",
    "android:build-release": "npm run cap:sync && cd android && ./gradlew bundleRelease",
    "android:build-apk": "npm run cap:sync && cd android && ./gradlew assembleRelease",
    "import:exercises": "node scripts/import-exercises.js",
    "import:exercises:dry": "node scripts/import-exercises.js --dry-run",
    "upload:videos": "node scripts/upload-exercise-videos.js",
    "upload:videos:list": "node scripts/upload-exercise-videos.js --list-unmatched"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.3",
    "csv-parse": "^5.5.6",
    "@capacitor/android": "^7.4.4",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@capacitor/ios": "^7.4.4",
    "@supabase/supabase-js": "^2.86.0",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.1.1",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
